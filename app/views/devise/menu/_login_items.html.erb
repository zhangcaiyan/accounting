<% if user_signed_in? %>
  <li>
  <%= link_to('退出', destroy_user_session_path, method: :delete, class: "logout") %>        
  </li>
<% else %>
  <li>
  <%= link_to('登陆', new_user_session_path, class: "login") %>  
  </li>
<% end %>

